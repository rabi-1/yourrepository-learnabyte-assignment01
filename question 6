#include<stdio.h>
int main()
{
	int n;int arr[n];
	int i;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
    for(i=2;i<n/2;i++)
    {
    	if(n%i==0)
    	printf("the number id not prime");
    	else
    	printf("%d",arr[i]);
	}
}
